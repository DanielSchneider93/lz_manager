<h3>Edit Group <%= @group.title%></h3>

<%= form_with(model: @group) do |form| %>
  <p>
    <%= form.text_field :title, id:"edit_field_id" %>
  </p>
  <p>
    <%= form.submit %>
  </p>

<% end %>

<%= link_to 'Back', groups_path %>
